# Streamlit Cloud Secrets Configuration Template
# Copy this file to .streamlit/secrets.toml for local development
# In Streamlit Cloud, add these as secrets in the dashboard

# Required for production deployment
ENVIRONMENT = "development"  # Set to "production" in Streamlit Cloud
SECRET_KEY = "local-dev-secret-key-12345"

# Optional: Model URL for cloud deployment (if model.pth is too large for git)
# MODEL_URL = "https://drive.google.com/uc?export=download&id=YOUR_FILE_ID"

# Optional: Database URL for production (SQLite resets on each deployment)
# DATABASE_URL = "postgresql://user:pass@host:port/dbname"

# Optional: Payment integration
# STRIPE_SECRET_KEY = "sk_test_..."
# PAYPAL_CLIENT_SECRET = "..."

# Optional: Storage configuration
# AWS_ACCESS_KEY_ID = "..."
# AWS_SECRET_ACCESS_KEY = "..."
# S3_BUCKET_NAME = "your-bucket"

# ⚠️ SECURITY WARNING:
# This file contains sensitive information and should NOT be committed to git
# It's already excluded in .gitignore